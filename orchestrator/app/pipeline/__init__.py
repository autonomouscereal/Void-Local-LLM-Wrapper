from __future__ import annotations

# The pipeline package provides narrowly scoped, pure or near-pure helpers
# used by the /v1/chat/completions coordinator. Keep imports at top and avoid
# importing from app.main to prevent circular dependencies.


