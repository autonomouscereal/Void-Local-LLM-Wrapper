PUBLIC_BASE_URL=http://localhost:8000

# Paths
UPLOAD_DIR=/workspace/uploads
STATE_DIR=/workspace/uploads/state
DATASETS_ROOT=/workspace/uploads/artifacts/datasets
REF_ROOT=/workspace/uploads/artifacts/refs
PROMPTS_ROOT=/workspace/uploads/artifacts/prompts

# Orchestrator core
ICW_MODE=committee
ICW_DISABLE=0
ABLATE=on
ABLATE_EXPORT=on
ABLATE_SCOPE=auto
AUTO_EXECUTE_TOOLS=true
ALLOW_TOOL_EXECUTION=true


# Film-2 QA thresholds
FILM2_QA_T_FACE=0.85
FILM2_QA_T_VOICE=0.85
FILM2_QA_T_MUSIC=0.80

# ComfyUI settings
SCENE_SUBMIT_CONCURRENCY=2
COMFYUI_BACKOFF_MS=250
COMFYUI_BACKOFF_MAX_MS=4000
COMFYUI_MAX_RETRIES=6

# Core service URLs
COMFYUI_API_URL=http://comfyui:8188
# COMFYUI_API_URLS=http://comfyui-a:8188,http://comfyui-b:8188
OCR_API_URL=http://ocr:8070
VLM_API_URL=http://vlm:8050
WHISPER_API_URL=http://whisper:9090
XTTS_API_URL=http://xtts:8020
MUSIC_API_URL=http://music:7860
YUE_API_URL=http://yue:9001
SAO_API_URL=http://sao:9002
DEMUCS_API_URL=http://demucs:9101
HUNYUAN_VIDEO_API_URL=http://hunyuan:9007
HUNYUAN_FOLEY_API_URL=

# Postgres / RAG
POSTGRES_HOST=postgres
POSTGRES_DB=wrapper
POSTGRES_USER=wrapper
POSTGRES_PASSWORD=wrapper

# Optional search
# SERPAPI_API_KEY=your_key

# NVIDIA GPU enablement (requires NVIDIA container runtime)
CONTAINER_RUNTIME=runc
OLLAMA_RUNTIME=nvidia
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=compute,utility
NVIDIA_DISABLE_REQUIRE=1
GPU_DEVICES=all
OLLAMA_QWEN_GPU=0
OLLAMA_GPTOSS_GPU=1

# HF transfer (optional)
HF_HUB_ENABLE_HF_TRANSFER=1

# CUDA wheels channel for GPU builds (use cu126 for Pascal-safe PyTorch 2.8)
PIP_EXTRA_INDEX_URL=https://download.pytorch.org/whl/cu126

# ----------------- Models & Planner -----------------
# Core LLMs
QWEN_MODEL_ID=qwen3:32b-instruct-q6_K
GPTOSS_MODEL_ID=chatgpt-oss:latest

# Default sampling
DEFAULT_NUM_CTX=8192
DEFAULT_TEMPERATURE=0.3

# Planner / Debate / Web
PLANNER_MODEL=qwen
ENABLE_DEBATE=true
MAX_DEBATE_TURNS=1
ENABLE_WEBSEARCH=true
ALLOW_TOOL_EXECUTION=true

# Postgres overrides (if you use different host/db)
POSTGRES_HOST=pgvector
POSTGRES_PORT=5432
POSTGRES_DB=ragdb
POSTGRES_USER=rag
POSTGRES_PASSWORD=ragpass

# Embeddings & RAG
EMBEDDING_MODEL_NAME=BAAI/bge-large-en-v1.5
RAG_CACHE_TTL_SEC=300

# FaceID/locks
FACEID_API_URL=http://faceid:7000

# Whisper / XTTS models
WHISPER_MODEL_NAME=large-v3
WHISPER_CPU_THREADS=8
XTTS_MODEL_NAME=tts_models/multilingual/multi-dataset/xtts_v2

# Music / VLM models
MUSIC_MODEL_ID=facebook/musicgen-small
VLM_MODEL_ID=Qwen/Qwen2-VL-7B-Instruct

# Tools auto-exec
# Tools auto-exec
AUTO_EXECUTE_TOOLS=true

# Tokens / API keys (fill in locally)
HUGGINGFACE_TOKEN=<your_hf_token>
SERPAPI_API_KEY=

# Public base URL
PUBLIC_BASE_URL=http://192.168.50.222:8000

